// Placeholder for local dev / `vite preview`. In Docker this file is
// generated by docker-entrypoint.sh from CONFIGLENS_API_BASE_URL at
// container start, so the same built bundle can point at different API
// URLs per environment without being rebuilt.
window.__CONFIGLENS_API_BASE__ = window.__CONFIGLENS_API_BASE__ || ''
